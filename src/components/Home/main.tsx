import { SearchSection } from "./SeactSection";

export function Main() {
    return (
        <main className='w-screen h-screen bg-hero-pattern bg-no-repeat bg-cover flex flex-col items-center justify-center'>
            <SearchSection/>
        </main>
    )
}